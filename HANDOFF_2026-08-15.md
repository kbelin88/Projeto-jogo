# HANDOFF — 15/08/2026 (ponto salvo)

Snapshot de estado para retomar o projeto ou levar a outro chat/modelo. Para o que o
projeto É (arquitetura, mecânica, convenções), ver **`CLAUDE.md`**. Este documento é
*onde estamos e o que vem a seguir*.

---

## 1. O que aconteceu nesta fase (ordem cronológica)

1. **OpenRouter virou conta paga** ($10). Medimos o custo real de raciocínio por modelo
   e montamos o browser para partidas frontier com medidor de custo ao vivo + freio de
   gasto ($7) + auto-save por turno (checkpoint no disco via `servir.py`).
2. **Torneio / partidas frontier:**
   - DeepSeek R1 vs Gemini 3.1 Flash-lite: R1 dominou o mapa (24 aldeias) em 57 turnos.
     R1 é o **melhor jogador do benchmark** (candidato a degrau 3: concentração + tempo).
   - qwen3-235b-thinking vs nemotron-3-super-120b (14/08): qwen venceu por eliminação em
     **101 turnos**. Essa partida revelou 4 problemas → viraram os LOTES C e D.
3. **LOTES A→B→C→D** (specs do próprio Lucas, executados aqui). Ver `RELATORIO_LOTE_C.md`
   e `RELATORIO_LOTE_D.md`. Resumo em `CLAUDE.md` §7.
4. **QoL/robustez:** hover (tropas ao passar o mouse em aldeia/exército), auto-save,
   correção do badge de CI (smokes de arena que quebraram na fase do mapa iberia).
5. **Estudos (Módulo 3):** Lucas **PUBLICOU no LinkedIn** — fechou a Fase 11, o
   anti-padrão que adiava há um mês. Ver memória `estudos-modulo3-base`.
6. **Revisão Fable 5 + LOTE E (fairness).** O Fable 5 revisou `engine.js` + `index.html`
   (pacote em `REVISAO_FABLE5.md`) e devolveu a SPEC_LOTE_E (achados A1–A6). Executado
   INTEIRO — 11 commits, branch `spec-lote-e-fairness`. Ver `RELATORIO_LOTE_E.md`.
   **Manchete (A1):** no caminho antigo o Rei B lia os envios que A ordenava no MESMO
   turno (`montarVisao` inclui `transito`, sem fog of war) — assimetria de MEDIÇÃO do
   benchmark. Corrigido no motor (E1) e no browser (E2): as duas ordens saem da mesma
   fotografia. + interceptação na chegada (A3), desempate de estrada sem viés de assento
   (A4), duração da chamada `| ms N` no log (A2, p/ diagnosticar os 23 vazios), teto de
   custo configurável (A6, `gteto`), 4 métricas novas no analisador (E7), `.gitattributes`
   (E9). Suíte verde, regressão byte-idêntica com flags off, crescimento de prompt ZERO.

## 2. Estado do código

- **Branch de trabalho:** `spec-lote-e-fairness` (11 commits sobre `spec-lote-d-memoria`;
  tem TUDO: LOTE A→E + torneio + fixes). Linhagem: `...→ spec-lote-c-visao →
  spec-lote-d-memoria → spec-lote-e-fairness`. `main` local está velha (`1ee5cc2`,
  03-04/08), ~60 commits atrás. Cada branch de lote tem um `RELATORIO_LOTE_*.md`.
- **Verde:** motor 25/25 (inclui `test_lote_e.js` 5/5 e `test_lote_c.js` 7/7), 5 smokes
  de arena, `verificarEquilibrio()` = 0. Regressão byte-idêntica (texto via `ref-lote-c`,
  comportamento via `ref-lote-e`) com todas as flags B/C/D/E off.
- **Não pushado** (branches locais). `CLAUDE.md`, este handoff e os `RELATORIO_LOTE_*`
  estão na `spec-lote-e-fairness`.
- **Novo flag em jogo (LOTE E):** as três flags de motor `ordensSimultaneas`,
  `interceptaChegada`, `desempateEstradaRng` são default-ON. Para reproduzir o motor
  pré-E (comparar com partidas antigas) põe-se as três a `false`.

## 3. Orçamento OpenRouter

- **Restante: ~$4.18** de $10 (antes do smoke; o smoke de 3 turnos deve custar ~$0.15).
  Raciocinadores pesados pagos: ~$0.03/turno/lado. DeepSeek R1 out $2.50/M, Qwen3-235B
  out $2.30/M.
- **Reservado** para a partida ao vivo DeepSeek R1 × Qwen (o "tiro final"). A análise do
  Fable 5 saiu dos créditos de uso do Lucas, não da API. **Baixar o `teto $` p/ ~4** na
  partida ao vivo — o saldo não cobre uma partida longa de dois raciocinadores a preço
  cheio (o de 101 turnos custou ~$2.35).

## 4. Próximo passo IMEDIATO: a partida ao vivo (DeepSeek R1 × Qwen3-235B)

A análise do Fable 5 já foi feita (virou o LOTE E, §1.6). O próximo passo é o **"tiro
final" na OpenRouter**: DeepSeek R1 (A) × Qwen3-235B-thinking (B), ao vivo no browser.
- **Antes da partida:** foi rodado um smoke de 3 turnos (headless runner) p/ confirmar
  que os modelos respondem bem com o código do LOTE E (parsing, log `| ms N`, sem crash).
- **Setup da partida ao vivo:** `python servir.py` → `localhost:8000` no Edge. Selecionar
  DeepSeek R1 e Qwen3-235B-thinking, **sem limite de turnos**, e conferir o `teto $`
  (novo campo `gteto`, default 7) — como o saldo é ~$4.18, **baixar o teto p/ ~4** para o
  freio realmente frear. O log/replay/custo salvam no fim (+ checkpoint por turno).
- Os modelos veem o MESMO prompt P2 de sempre (o LOTE E não mexeu no prompt). O que mudou
  para a partida: a ordem simultânea (E1/E2), o combate de estrada (E3/E4), o `| ms N` no
  log, e o teto configurável.

## 5. Decisões e threads abertas

- **23 respostas vazias (13/08): causa ainda DESCONHECIDA, mas o instrumento está pronto.**
  O D1 (finish/nativo/erroApi) + E5 (`| ms N`) + E7 (o analisador separa `ms` de turnos
  vazios vs não-vazios) dão o teste da hipótese direto no JSON. Predição pré-registrada
  (corte por tempo) em `RELATORIO_LOTE_E.md` §4. Confirmação vem da **próxima partida real**
  no browser — a do DeepSeek R1 × Qwen que o Lucas vai rodar. Validado no log de 13/08:
  B vazios=23, sem-usage=2.
- **O RUNNER headless (`rei_vs_rei.js`) NÃO tem a correção de fairness (A1).** O E2 foi
  só no browser; o runner é um TERCEIRO caminho de decisão (decide A→executa→decide B, B
  vê os envios de A). Fica para o lote do cliente unificado (já estava fora de escopo do
  LOTE E). As partidas ao vivo (browser) e os testes unitários (E8.2) têm o fix; smokes
  via runner, não.
- **Monocultura de arqueiro (99.7%):** problema de economia conhecido; o LOTE C/D não
  mexeu (fora de escopo, mantém comparabilidade). Fica p/ um lote de economia — com
  gabarito antes.
- **`main` a consolidar:** a árvore de branches (~18 branches) precisa de limpeza; a
  `main` deveria receber pelo menos o LOTE A→D. Decisão do Lucas quando/como mergear.
- **Cliente OpenRouter duplicado** (`index.html` vs `rei.js`): refactor grande, adiado.
- **Revogar as 2 chaves de API expostas em 03/08** — tarefa manual do Lucas, antes de
  carregar mais crédito.
- **Harness de 2 assentos:** partidas single-seat (um assento, uma seed) não dão
  afirmação comparativa rigorosa. Para o "Relatório #2", rodar A×B e B×A.

## 6. Estudos (Módulo 3 "Do Benchmark ao Produto")

Fase 11 (publicar) FECHADA. Muito do trabalho recente foi Fase 13 (custo/roteamento) na
prática. Próximo natural: consolidar o achado publicado + decidir os 6 pontos da "sessão
de revisão" que o `modulo3_base.txt` pede. Ver memória `estudos-modulo3-base`.

## 7. Se você é um modelo novo chegando agora

Leia `CLAUDE.md` primeiro. O código vivo está na branch `spec-lote-d-memoria`. Rode
`python servir.py` e abra `localhost:8000` para ver o jogo. Não toque em combate,
topologia do mapa, nem ponha rodapé de sessão nos commits (repo público).
