# HANDOFF — 16/08/2026 (ponto salvo)

Snapshot para retomar o projeto na próxima sessão. Para o que o projeto É (arquitetura,
mecânica, convenções), ver **`CLAUDE.md`**. O handoff anterior é `HANDOFF_2026-08-15.md`
(estado pré-v4). Este é *onde paramos e o que vem a seguir*.

---

## 1. O que aconteceu nesta sessão (16/08)

1. **Recuperação do crash.** A partida qwen3-235b-thinking (A) × deepseek-r1 (B) de 15/08
   travou por crash do PC no ~turno 34. **O auto-save por turno (`/checkpoint`) sobreviveu**
   com os 38 turnos completos, em
   `checkpoints/partida_openrouter-qwen-...-deepseek-r1_2026-08-15-17-49.txt` (gitignored).
   O download em `~/Downloads/...21-09.txt` só tinha até o turno 23. **Falta o replay .json
   da fase final** (só até turno 23), então métricas de motor da reta final não existem.
2. **Análise a fundo dessa partida** (base do reboot). Achados:
   - **Monocultura de arqueiro é EQUILÍBRIO racional, não burrice.** Arqueiro é o
     generalista sem fraqueza (atq2/def2); como os dois só fazem arqueiro, o triângulo
     nunca dispara → o eixo estratégico central fica MORTO. O benchmark media menos do que
     prometia.
   - **Impasse:** vitória só por eliminação + bônus defensivo + desempate pró-defensor →
     partidas empatam (burro-vs-burro empatava 2 de 3 seeds no v3).
   - **Qwen deixava tropa apodrecer na retaguarda** (Porto: casa 23-28 ociosas) enquanto
     DeepSeek reforçava a frente quase todo turno (casa ~0). Motivo: Porto a ~13 turnos da
     frente. DeepSeek roda pipeline logístico; Qwen não.
   - DeepSeek R1 pensa **~6 min/turno** — o "lento no relógio" é o modelo, não o mapa.
3. **RULESET V4 — reboot de balanceamento** (Lucas aprovou fechar a comparabilidade).
   6 mudanças, opt-in via `Engine.CONFIG_V4` (o `CONFIG` congelado segue default e
   byte-idêntico). Ver memória `ruleset-v4-reboot` e `CLAUDE.md` §7. Toggle no browser
   (**⚔️ regras v4**, `#gv4`) e 7º arg `v4` no runner headless.
4. **Smoke v4 (grátis, free tier): Gemini × Nemotron 120b, 20 turnos.** Resultado que
   VALIDOU o reboot:
   - **Nemotron montou exército MISTO: 72L / 39A / 5C** — diversificação e cavaleiros no
     tabuleiro pela 1ª vez na história do benchmark. O counter 1.5 + cavaleiro viável
     funcionou num modelo competente.
   - **Gemini caiu em lanceiro-mono (160/0/0)** — o modo de falha da Fase 4 (madeira 15
     torna lanceiro spammável a 0 ferro). MAS o v4 **pune**: placar final Gemini 21 tropas
     × Nemotron 91. Quem monocultiva está PERDENDO — o benchmark voltou a discriminar.
   - Distâncias curtas: **neutras=0 no turno 20** (no v3 era ~turno 33). 0 erros de rede.

## 2. Estado do código

- **Branch:** `spec-lote-e-fairness`. O v4 foi **commitado** aqui (engine.js, index.html,
  runners/rei_vs_rei.js, testes/test_regras_v4.js).
- **Verde:** motor **26/26** (inclui `test_regras_v4.js` 12/12), 5 smokes de arena,
  `verificarEquilibrio()` = 0, `index.html` carrega. Caminho antigo (CONFIG) byte-idêntico.
- **Cuidado com fim-de-linha:** o repo é **LF** (`.gitattributes * -text`). O editor no
  Windows tende a gravar CRLF — se o `git diff` mostrar o arquivo INTEIRO mudado, é isso;
  normalizar com `sed -i 's/\r$//' <arquivo>` antes de commitar.
- **Não pushado** (branches locais).

## 3. Orçamento OpenRouter

- **Restante: ~$2.32** de $10 (conferido via API em 16/08; usado $7.68). Dá pra ~1 partida
  do porte de 2 reasoners (~$1.7), NÃO duas. Ver memória `handoff-partida-crash-0815`.
- O smoke Gemini×Nemotron foi **free tier** (custo $0).

## 4. Próximo passo IMEDIATO: partida ao vivo DeepSeek R1 × Qwen3-235B com REGRAS V4

Lucas vai reiniciar o PC e rodar no browser (quer assistir). Setup:
- `python servir.py` → `localhost:8000/index.html`. **NÃO** abrir por duplo clique.
- Selecionar DeepSeek R1 × Qwen3-235B-thinking, **marcar ⚔️ regras v4**, conferir o
  `teto $` (baixar p/ ~2 dado o saldo), e escolher os turnos.
- **Para ver um VENCEDOR por dominância (75%/2t), usar "∞ sem limite" ou ~40-50 turnos**
  — 20 turnos não chegam a 75%. Com distâncias curtas deve resolver mais rápido que os 38+
  do v3. Lembrar: DeepSeek a ~6 min/turno × 40+ turnos = horas.
- **A pergunta do teste:** sob counter 1.5 + cavaleiro viável, dois reasoners fortes
  montam exército MISTO (como o Nemotron fez)? Confirmar diversificação + jogo rei-vs-rei.
- O log grava `regras=v4 (...)` no cabeçalho — honesto sobre o ruleset.

## 5. Threads abertas (atualizadas)

- **Monocultura:** ENDEREÇADA pelo v4 (não mais "fora de escopo"). Falta confirmar em dois
  reasoners fortes na partida paga.
- **Madeira 15 tenta lanceiro-spam** (Fase 4): confirmado no smoke com o Gemini. Knob de
  ajuste se incomodar = madeira 12. Lucas decidiu MANTER 15 (o v4 pune a mono de qualquer
  jeito).
- **Custo conhecido do counter 1.5:** 1 cavaleiro deixa de tomar 1 lanceiro no castelo
  (2×1.5×1.5=4.5 > atq 4). Contradiz o "requisito do Lucas" antigo, de propósito; reversível.
- **Gap: desempate por nº de aldeias no teto de turnos** entrou só no `rodarPartida`
  (headless); o loop do BROWSER ainda diria "empate" se ninguém chegar a 75% (baixo risco —
  a vitória por 75% dispara via `checarVitoria`, que o browser chama).
- **23 respostas vazias (13/08):** instrumento pronto (D1/E5/E7); confirmação vem da
  próxima partida real. Ver `RELATORIO_LOTE_E.md` §4.
- **Runner headless AGORA aceita v4** (7º arg), mas segue SEM a correção de fairness A1
  (terceiro caminho de decisão; fora de escopo, adiado com o cliente unificado).
- **`main` a consolidar** (~18 branches); **revogar 2 chaves expostas em 03/08**; cliente
  OpenRouter duplicado (browser vs runner) — refactor adiado.
- **Harness de 2 assentos:** partidas single-seat (uma seed, um assento) não dão afirmação
  comparativa rigorosa. Para um "Relatório #2" sério, rodar A×B e B×A (troca de lado).
- **Estudos (Módulo 3 "Do Benchmark ao Produto"):** Fase 11 (publicar) FECHADA (Lucas
  publicou no LinkedIn). O reboot v4 é trabalho de balanceamento/produto na prática. Ver
  memória `estudos-modulo3-base`.

## 6. Se você é um modelo novo chegando agora

Leia `CLAUDE.md` primeiro. Branch viva: `spec-lote-e-fairness`. Rode `python servir.py` e
abra `localhost:8000`. O ruleset v4 é opt-in (`CONFIG_V4` / toggle `gv4` / arg `v4`); o
`CONFIG` antigo segue congelado e byte-idêntico. Não ponha rodapé de sessão nos commits
(repo público). Cuidado com CRLF no Windows (§2).
