# LOTE E, E9 (achado B3): a qualidade central deste repo e a comparacao byte a byte
# contra baselines de texto (testes/ref-lote-c/) e de comportamento (testes/ref-lote-e/).
# Um clone com core.autocrlf diferente converteria fins de linha e quebraria a
# regressao por fantasma. `* -text` CONGELA os bytes como estao — SEM normalizacao
# (nao usar text=auto nem eol=, que reescreveriam arquivos e invalidariam as refs).
* -text
