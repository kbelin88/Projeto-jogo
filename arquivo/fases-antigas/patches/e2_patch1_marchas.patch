diff --git a/index.html b/index.html
index ab642a6..ac1128a 100644
--- a/index.html
+++ b/index.html
@@ -1501,6 +1501,29 @@
     ctx.fillRect(sx - r * 0.10, y + h * 0.35, r * 0.20, h * 0.65);
   }
 
+  // escudo do exercito em marcha (E2): peca na cor do rei — le-se
+  // "tropa no tabuleiro", nao projetil. Arte heraldica de verdade e E5;
+  // aqui e o slot minimo: escudo chapado + contorno.
+  function desenharEscudo(x, y, r, col) {
+    const w = r * 1.7, h = r * 2.1;
+    ctx.save();
+    ctx.beginPath();
+    ctx.moveTo(x - w / 2, y - h * 0.42);
+    ctx.lineTo(x + w / 2, y - h * 0.42);
+    ctx.quadraticCurveTo(x + w / 2, y + h * 0.12, x, y + h * 0.58);
+    ctx.quadraticCurveTo(x - w / 2, y + h * 0.12, x - w / 2, y - h * 0.42);
+    ctx.closePath();
+    ctx.fillStyle = col; ctx.fill();
+    ctx.lineWidth = Math.max(1.5, r * 0.16); ctx.strokeStyle = "rgba(20,12,4,.8)";
+    ctx.stroke();
+    ctx.lineWidth = Math.max(1, r * 0.09); ctx.strokeStyle = "rgba(255,255,255,.35)";
+    ctx.beginPath();                                  // filete interno: relevo
+    ctx.moveTo(x - w * 0.34, y - h * 0.30);
+    ctx.lineTo(x + w * 0.34, y - h * 0.30);
+    ctx.stroke();
+    ctx.restore();
+  }
+
   // desenha aldeias do jogo (cor do dono) + exercitos em transito
   // ---- TERRITORIO DE TRANSMISSAO: influencia pintada + CONTORNO ----
   // Direcao do handoff: "bordas de territorio com contorno (nao so
@@ -1632,31 +1655,12 @@
       const dx = d.x * cs + panX + cs / 2, dy = d.y * cs + panY + cs / 2;
       const col = COR[m.dono];
 
-      // fio fino ate o destino (intencao) + RASTRO esmaecente atras (movimento)
-      ctx.lineWidth = 1; ctx.strokeStyle = "rgba(0,0,0,.15)";
-      ctx.beginPath(); ctx.moveTo(mx, my); ctx.lineTo(dx, dy); ctx.stroke();
-      const ox = o.x * cs + panX + cs / 2, oy = o.y * cs + panY + cs / 2;
-      const angT = Math.atan2(my - oy, mx - ox);
-      const compR = Math.min(Math.hypot(mx - ox, my - oy), cs * 2.0);
-      const tx = mx - Math.cos(angT) * compR, ty = my - Math.sin(angT) * compR;
-      const gradR = ctx.createLinearGradient(tx, ty, mx, my);
-      const rgbR = m.dono === "A" ? "93,140,240" : "239,93,93";
-      gradR.addColorStop(0, `rgba(${rgbR},0)`);
-      gradR.addColorStop(1, `rgba(${rgbR},.55)`);
-      ctx.lineWidth = Math.max(2, cs * 0.10); ctx.lineCap = "round";
-      ctx.strokeStyle = gradR;
-      ctx.beginPath(); ctx.moveTo(tx, ty); ctx.lineTo(mx, my); ctx.stroke();
-      ctx.lineCap = "butt";
-
-      // PREVISAO DE CONFRONTO: a 1 turno de chegar, halo no alvo diz o
-      // desfecho (verde vence / vermelho perde / ambar parelho). Usa a
-      // MESMA conta do motor (Engine.preverCombate) — nunca diverge.
+      // ALERTA DE ATAQUE (E2): a 1 turno de chegar, halo na COR DO
+      // ATACANTE marca o alvo — a ameaca tem dono, o desfecho fica em
+      // aberto. Decisao de 16/07: previsao de resultado tira a tensao
+      // do espectador (a conta do motor saiu da UI; segue no eval).
       if (m.turnosRestantes === 1 && d.dono !== m.dono && cs > 13) {
-        const pv = Engine.preverCombate(game, m.tropas, d);
-        const folga = 1.2;
-        const corH = pv.atacanteVence
-          ? (pv.FatkEf > pv.FdefEf * folga ? "#7dc95e" : "#e0b84a")
-          : (pv.FdefEf > pv.FatkEf * folga ? "#e05a4a" : "#e0b84a");
+        const corH = col;
         const rh = Math.max(8, cs * 0.52);
         ctx.save();
         ctx.shadowColor = corH; ctx.shadowBlur = Math.max(6, cs * 0.35);
@@ -1678,9 +1682,12 @@
       ctx.save();
       ctx.beginPath(); ctx.moveTo(mx, my); ctx.lineTo(dx, dy);
       if (ehAtaque) {
-        ctx.globalAlpha = m.turnosRestantes === 1 ? 0.75 : 0.40;
-        ctx.lineWidth = Math.min(cs * 0.26, Math.max(2, cs * 0.08 * Math.sqrt(Fm / 50)));
-        ctx.strokeStyle = col;
+        // E2: rota de ataque = pontilhado PRETO fino, do ponto atual ao
+        // alvo. A cor mora no escudo; a linha so aponta. Firma a 1 turno.
+        ctx.globalAlpha = m.turnosRestantes === 1 ? 0.60 : 0.42;
+        ctx.lineWidth = Math.max(1, cs * 0.045);
+        ctx.setLineDash([Math.max(2, cs * 0.12), Math.max(4, cs * 0.22)]);
+        ctx.strokeStyle = "rgba(12,9,4,.9)";
       } else {
         ctx.globalAlpha = 0.13;
         ctx.lineWidth = 1;
@@ -1689,19 +1696,7 @@
       }
       ctx.stroke();
       ctx.restore();
-      ctx.fillStyle = col;
-      ctx.beginPath(); ctx.arc(mx, my, r, 0, Math.PI * 2); ctx.fill();
-      ctx.lineWidth = 1.5; ctx.strokeStyle = "rgba(0,0,0,.65)"; ctx.stroke();
-
-      // seta: para onde o exercito esta indo
-      const ang = Math.atan2(dy - my, dx - mx);
-      ctx.fillStyle = col;
-      ctx.beginPath();
-      ctx.moveTo(mx + Math.cos(ang) * (r + 7), my + Math.sin(ang) * (r + 7));
-      ctx.lineTo(mx + Math.cos(ang + 2.5) * (r + 1), my + Math.sin(ang + 2.5) * (r + 1));
-      ctx.lineTo(mx + Math.cos(ang - 2.5) * (r + 1), my + Math.sin(ang - 2.5) * (r + 1));
-      ctx.closePath(); ctx.fill();
-      ctx.lineWidth = 1; ctx.strokeStyle = "rgba(0,0,0,.5)"; ctx.stroke();
+      desenharEscudo(mx, my, r, col); // E2: a tropa e uma peca, nao um ponto
 
       // rotulo do exercito: icone do tipo sempre que da; NUMERO so de perto
       // (de longe, o TAMANHO do marcador ja conta a forca)
